webdriver{
driver = chrome
}

serenity.take.screenshots = FOR_FAILURES

headless.mode = false

serenity{
project.name = "Myntra Brand availability"
test.root = "src.test.resources.features"
tag.failures = "true"
restart.browser.for.each = example
}

environment{

sit{
   baseurl = "https://www.myntra.com/"
  }

}